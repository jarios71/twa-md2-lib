import { OnChanges, SimpleChanges } from '@angular/core';
import { NgxMaterialTimepickerHoursFaceComponent } from '../timepicker-hours-face/ngx-material-timepicker-hours-face.component';
import { TimePeriod } from '../../models/time-period.enum';
import { ClockFaceTime } from '../../models/clock-face-time.interface';
export declare class NgxMaterialTimepicker12HoursFaceComponent extends NgxMaterialTimepickerHoursFaceComponent implements OnChanges {
    period: TimePeriod;
    constructor();
    readonly disabledHours: ClockFaceTime[];
    ngOnChanges(changes: SimpleChanges): void;
}
