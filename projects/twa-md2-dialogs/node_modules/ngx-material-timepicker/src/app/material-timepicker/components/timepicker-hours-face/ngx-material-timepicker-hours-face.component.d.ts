import { EventEmitter } from '@angular/core';
import { ClockFaceTime } from '../../models/clock-face-time.interface';
import { Moment } from 'moment';
export declare abstract class NgxMaterialTimepickerHoursFaceComponent {
    selectedHour: ClockFaceTime;
    minTime: Moment;
    maxTime: Moment;
    hourChange: EventEmitter<ClockFaceTime>;
    hourSelected: EventEmitter<null>;
    hoursList: ClockFaceTime[];
    constructor(hours: number);
    readonly abstract disabledHours: ClockFaceTime[];
    onClick(): void;
    initHours(hours: number): void;
}
